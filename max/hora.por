programa {
  funcao inicio() {
    real qtdrs, valor_hora, pagamento
    valor_hora = 35.00

    escreva("quantas horas trabalhadas? ")
    leia(qtdrs)

    pagamento = valor_hora * qtdrs

    escreva(" O pagamnento do mes será ? ", pagamento)
  }
}